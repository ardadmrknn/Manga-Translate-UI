Pillow
mss
pytesseract
opencv-python
numpy
keyboard
google-generativeai
google-cloud-translate
google-cloud-vision
google-cloud-bigquery
litert-lm; platform_system != "Windows"

# External dependency required for OCR:
# Install the Tesseract OCR application separately and add it to PATH.

# Note for local Gemma translation:
# On non-Windows, LiteRT-LM can be used with a .litertlm model file.
# On Windows, local_gemma can run through Ollama (install Ollama and pull a Gemma model).